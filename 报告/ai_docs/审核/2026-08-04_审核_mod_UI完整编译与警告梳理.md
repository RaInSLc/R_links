# mod_UI 完整编译与警告梳理

> 生成日期: 2026-08-04 | 类型: 审核 | 状态: 已确认

## 背景

本次任务针对 `mod_UI` 单文件拆分后的完整工程质量进行复核。前一阶段已将生产前端和 Rust 文件控制在 500 行以内，但此前只执行了 `cargo test`、前端测试和前端构建，未执行完整的 `npm run tauri build`，同时 release 编译暴露大量 Rust warning，需要补充验证并完成警告治理。

## 问题分析

### 现状

首次完整 `npm run tauri build` 能够成功生成 Windows 安装包，但 Rust release 编译产生 144 个源码级 warning。警告集中在拆分后的 Rust 模块，主要表现为：

- 原 `logic.rs` 的 import、常量和 `OnceLock<Regex>` 静态定义被复制到多个职责模块。
- `search.rs` facade 与 `search_context.rs` 同时保留实现依赖，产生重复无效导入。
- 测试专用函数和 release 库代码共用重导出路径，清理时容易影响 `cargo test`。
- 部分辅助函数只由测试使用，在 release 库检查中被报告为 dead code。

### 根因

拆分过程优先保证文件规模和行为迁移，未同步建立模块级最小依赖清单。由于 Rust 的模块作用域独立，原文件级依赖不能整体复制到所有子模块；复制方式会造成 warning，直接删除又可能导致测试构建缺少符号。

## 核心发现

1. `npm run tauri build` 已实际执行并成功完成，生成 MSI 和 NSIS 两种 Windows 安装包。
2. 通过删除复制的冗余依赖、恢复模块实际需要的正则和常量，并为测试专用函数设置正确的条件编译边界，`cargo check --release` 已达到 0 个 warning。
3. 清理过程中曾出现测试编译错误：`sanitize_log_message` 被错误移除，已恢复为测试专用重导出；最终 `cargo test` 通过 206 项。
4. 前端 `npm test -- --run` 通过 150 项，`npm run build` 通过。
5. 当前生产 `src/**/*.ts(x)` 和 `src-tauri/src/**/*.rs` 文件均小于 500 行；测试文件 `src/utils.test.ts` 的 723 行不属于生产文件约束范围。

## 实施方案

### 方案概述

采用“编译器驱动的最小依赖清理”方案：先保留行为和模块边界，再根据 warning 和 error 精确调整 import、常量、静态变量以及测试重导出，最终以 release 检查、单元测试、前端构建和 Tauri 打包共同验收。

### 详细步骤

| 步骤 | 操作 | 负责模块 | 风险等级 |
|------|------|----------|----------|
| 1 | 执行完整 `npm run tauri build`，保存日志与产物 | mod_UI/Tauri | 低 |
| 2 | 统计并分类 Rust release warning | src-tauri/src | 低 |
| 3 | 删除复制到输入、脚本、结果、历史、URL 和搜索模块的无效依赖 | logic 子模块、search 子模块 | 中 |
| 4 | 恢复实际使用的正则、常量和测试专用重导出 | logic/search/storage | 中 |
| 5 | 运行 release 检查、Rust 测试、前端测试、前端构建和完整打包 | 全工程 | 低 |

### 预期效果

- release Rust 编译无源码级 warning。
- Rust 测试、前端测试和完整 Tauri 打包均可独立复现。
- 模块只保留实际需要的依赖，降低后续拆分和维护成本。
- 不改变 Tauri command、前端公开导出和用户功能行为。

## 风险评估

| 风险项 | 影响范围 | 可能性 | 缓解措施 |
|--------|----------|--------|----------|
| 测试专用重导出被误删 | Rust 单元测试编译 | 中 | 使用 `#[cfg(test)]` 明确边界，并以 206 项测试作为验收门槛 |
| 模块依赖误删导致编译失败 | Rust release/test 编译 | 中 | 每轮先执行 `cargo check --release`，再执行 `cargo test` |
| 安装包生成链路与普通构建不一致 | Windows 发布流程 | 低 | 使用完整 `npm run tauri build` 验证 MSI 和 NSIS 产物 |
| 历史工作区改动被误提交 | README 和历史文档 | 中 | 提交前只暂存本轮源码、CHANGELOG 和审核文档 |

## 最终建议

当前编译质量已达到本轮目标：release 检查无 warning，完整 Tauri 打包成功。后续新增 Rust 模块时，应禁止复制整段 import 区块；每个模块应维护最小依赖，并在提交前固定执行 `cargo check --release` 和 `npm run tauri build`。

## 后续行动

- [ ] 将 `cargo check --release` 纳入 Rust 重构后的固定验证项。
- [ ] 将 `npm run tauri build` 纳入发布前检查清单。
- [ ] 后续新增模块时同步补充对应模块级测试，避免只依赖 facade 测试。

---

*文档由 document_writer skill 自动生成*
