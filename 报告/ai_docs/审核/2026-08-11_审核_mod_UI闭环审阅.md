# mod_UI 闭环审阅

> 生成日期: 2026-08-11 | 类型: 审核 | 状态: 已确认

## 背景

本次对 `mod_UI` 前端、Tauri Rust 后端、现有变更记录及已归档工程文档进行联合审阅，并执行项目既有自动化测试。审阅目标是识别影响已声明功能闭环的实现缺口，确认当前构建与打包链路可执行。

## 问题分析

### 现状

项目已覆盖 R、R 二进制、Pip 与 Conda 包检索，包含输入规则、脚本生成、历史记录、离线缓存、诊断、依赖解析及发布工作流。前端自动化测试在审阅开始时通过 151 项，Rust 自动化测试通过 206 项。

### 根因

设置页提供自定义 R 库路径以支持隔离安装，但脚本生成逻辑仅在部分安装来源拼接 `lib` 参数。GitHub、Bioconductor Git、R-Forge 与本地 R 包归档路径未使用该设置，因此同一配置在不同来源下可能写入默认 R 库。

## 核心发现

1. 自定义 R 库路径参数存在跨来源传递不完整的问题，属于功能闭环缺口，已在本轮修复。
2. 输入的手动编辑、换行、剪贴板粘贴、文件导入与受控状态回写已有前端测试覆盖；前端测试 151 项通过。
3. Rust 输入解析、URL 约束、脚本生成、结果清洗、历史与存储恢复已有 206 项测试通过；本轮新增自定义库路径多来源回归覆盖。
4. 本轮完整 `npm run tauri build` 已成功生成 MSI 与 NSIS 安装包，构建与打包链路完成闭环。

## 实施方案

### 方案概述

在统一的 R 命令生成入口为 GitHub、Bioconductor Git、R-Forge 分支追加已转义的 `lib` 参数；本地归档安装分支使用相同设置。新增一次性回归测试，确认四个来源都生成目标库路径。

### 详细步骤

| 步骤 | 操作 | 负责模块 | 风险等级 |
|------|------|----------|----------|
| 1 | 补齐远程安装命令的 `lib` 参数 | `command_generation.rs` | 低 |
| 2 | 补齐本地归档安装命令的 `lib` 参数 | `script_generation.rs` | 低 |
| 3 | 增加四来源回归测试 | `logic_tests_5.rs` | 低 |
| 4 | 运行前端、Rust 与完整 Tauri 打包验证 | `mod_UI` | 中 |

### 预期效果

无论 R 包来源为 CRAN、GitHub、Bioconductor、R-Forge 或本地归档，配置的自定义 R 库路径均会传入生成的安装命令，隔离安装行为保持一致。

## 风险评估

| 风险项 | 影响范围 | 可能性 | 缓解措施 |
|--------|----------|--------|----------|
| 第三方安装函数不接受 `lib` 参数 | 特定安装来源脚本执行 | 低 | 使用对应 R 包公开安装函数的标准 `lib` 参数，并以生成脚本回归测试验证 |
| 本地环境缺少打包依赖 | Windows 安装包生成 | 中 | 执行完整 `npm run tauri build` 并保留命令输出作为验证依据 |
| 外部 R 运行时差异 | 用户实际安装阶段 | 中 | 安装脚本继续由用户审阅与执行，后续可补充真实 R 环境集成测试 |

## 最终建议

本轮应以完整 Tauri 打包成功作为交付门槛。除本轮修复的库路径缺口外，审阅未发现新的明确攻击路径或需要标记为安全问题的缺陷。后续重点应放在具备 R、Pip、Conda 实际运行时的端到端安装验证，而非继续扩大静态功能范围。

## 后续行动

- [x] 补齐所有 R 安装来源的自定义库路径传递。
- [x] 增加多来源脚本生成回归测试。
- [x] 完成完整 Tauri 打包并核对 MSI、NSIS 产物。
- [ ] 在具备实际 R 运行时的环境中执行各来源安装脚本端到端验证。

---

*文档由 document_writer skill 自动生成*
