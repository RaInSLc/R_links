# RLinks 闭环复查分析

> 生成日期: 2026-07-11 | 类型: 分析 | 状态: 已确认

## 背景

本次复查针对 `报告\`、`mod_UI\`、`CHANGELOG.md`、`release\`、`scratch\` 五个指定范围执行完整闭环核查。复查目标是确认前序发布链路、诊断导出、文档归档、临时目录隔离和变更记录是否已经闭环，并继续处理可直接落地的剩余问题。

当前项目主线仍为 `mod_UI` Tauri 2 + React + Rust 桌面应用。仓库中 `release\` 用作本地产物目录，`scratch\` 用作实验与外部克隆数据目录，`报告\ai_docs\` 用作正式文档归档目录。

## 问题分析

### 现状

1. `git status --short` 显示复查开始时工作区干净。
2. `release\` 当前为空目录，无占位 `latest.json` 或陈旧可执行文件。
3. `scratch\` 包含 DEPTH2 克隆、测试 HTML 与 C++ 验证文件，但该目录被 `.gitignore` 忽略，未进入 Git 追踪范围。
4. `mod_UI\tauri.key`、`mod_UI\tauri.key.pub`、`mod_UI\dist`、`mod_UI\node_modules`、`mod_UI\src-tauri\target` 未被 Git 追踪。
5. `报告\build_exe.ps1` 与 `报告\build_exe.bat` 已纳入 Git 追踪，本地脚本已明确只生成便携版，不生成自动更新清单。
6. `报告\ai_docs\` 已按类型子目录组织，前序根目录散落文档已完成归档。
7. `mod_UI` 三处版本一致：`package.json`、`src-tauri\tauri.conf.json`、`src-tauri\Cargo.toml` 均为 `0.1.9`。
8. CI 已覆盖版本一致性、前端测试、Rust fmt、Clippy、Rust 测试和 Tauri 无打包构建。
9. Release 工作流已包含 updater 清单生成与 `latest.json` 内容校验。

### 根因

前序主要问题已完成大部分收口，但 Release 工作流仍存在一个流程层面的边界：`tauri-action` 在 `releaseDraft: false` 时会直接创建正式 Release。即使后续 `latest.json` 校验已经前置到附加资产上传之前，`tauri-action` 生成的安装包与 updater 资产仍可能先对用户可见。该问题属于发布过程原子性不足，不是业务代码缺陷。

## 核心发现

1. **目录隔离已闭环**：`release\` 为空且被忽略，`scratch\` 未追踪，临时实验数据不会进入正式提交。
2. **密钥与构建产物未追踪**：本地 `tauri.key*`、`node_modules`、`dist`、`target` 未进入 Git 追踪范围。
3. **诊断与文档归档已闭环**：诊断导出增强已在 `CHANGELOG.md` 记录，正式文档已按类型子目录归档。
4. **发布流程仍需增强原子性**：Release 工作流需要从直接正式发布改为先创建草稿，待清单校验和附加资产上传完成后再发布。
5. **业务代码本轮无新增缺陷信号**：本轮复查未发现需要修改 `mod_UI` 前后端业务代码的新增问题。

## 实施方案

### 方案概述

本轮采用最小变更收口发布流程：将 Release 工作流改为草稿发布，完成 updater 清单校验、便携版可执行文件上传和 `latest.json` 上传后，再使用 GitHub CLI 将对应版本 Release 从草稿切换为正式发布。

### 详细步骤

| 步骤 | 操作 | 负责模块 | 风险等级 |
|------|------|----------|----------|
| 1 | 将 `tauri-action` 的 `releaseDraft` 调整为 `true` | `.github\workflows\release.yml` | 低 |
| 2 | 附加资产上传保持草稿状态，避免中间资产提前对用户可见 | `.github\workflows\release.yml` | 低 |
| 3 | 使用 `package.json` 版本计算 `v{version}`，在校验和上传完成后发布正式 Release | `.github\workflows\release.yml` | 中 |
| 4 | 更新 `CHANGELOG.md`，记录草稿发布闭环与复查结果 | `CHANGELOG.md` | 低 |
| 5 | 保存本次复查分析文档 | `报告\ai_docs\分析\` | 低 |

### 预期效果

1. Release 中间态不会直接暴露给用户。
2. `latest.json` 校验失败时，发布保持草稿状态，便于维护者修复或删除。
3. 正式发布前完成版本号、下载 URL、签名字段和附加资产上传校验。
4. 后续发布失败路径更容易回滚和排查。

## 风险评估

| 风险项 | 影响范围 | 可能性 | 缓解措施 |
|--------|----------|--------|----------|
| GitHub CLI 发布目标 tag 解析错误 | Release 保持草稿状态 | 低 | 使用 `package.json` 版本显式生成 `v{version}`，避免 `workflow_dispatch` 分支名误判 |
| `latest.json` 校验失败 | Release 不会正式发布 | 中 | 保持草稿发布，维护者可检查构建产物后重新运行工作流 |
| 附加资产上传失败 | Release 不完整 | 中 | 上传失败时不会执行正式发布步骤 |
| 本地 `scratch\` 目录持续膨胀 | 本地磁盘占用增加 | 中 | 保持忽略策略，必要时人工清理，不纳入业务提交 |

## 最终建议

当前项目主线闭环状态较前序检查明显改善。`release\`、`scratch\`、密钥、构建产物和文档归档边界均已明确；需要继续关注的是发布流程的正式发版原子性。本轮已通过草稿发布机制收口该问题。

短期内不建议对 `mod_UI` 业务代码进行额外重构。后续优化重点应放在 Release 工作流实际跑通验证、复杂 UI 交互测试和后端 mock 网络测试补齐。

## 后续行动

- [ ] 下一次正式发版时检查 GitHub Actions Release 是否按“草稿构建 -> 校验 -> 上传 -> 发布”顺序执行。
- [ ] 若 Release 失败，确认草稿 Release 未被自动发布，并检查 `latest.json`、签名与附件 URL。
- [ ] 持续保持 `release\` 为空或仅存本地产物且不提交。
- [ ] 定期清理 `scratch\` 中不再需要的外部克隆和大体积实验数据。
- [ ] 补齐前端复杂视图交互测试和后端 mock 网络检索测试。

---

*文档由 document_writer skill 自动生成*
